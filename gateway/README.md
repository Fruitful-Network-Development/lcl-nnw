# Archived Gateway

This directory is archived and unsupported.

The active product surface for this repository is the `lcl` CLI at the repo root workspace. The former HTTP gateway flow is retained only as migration/reference code and should not be extended.
