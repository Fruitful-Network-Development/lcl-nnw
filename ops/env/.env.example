# Archived migration notice
#
# Runtime configuration now lives in:
#   .claw/settings.json
#   .claw/settings.local.json
#
# The old gateway/lane environment variables are retired and unsupported in the
# Claw-first `lcl` workflow. Keep this file only as a pointer during migration.
