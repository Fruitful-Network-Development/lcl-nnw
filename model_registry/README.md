# Archived Model Registry

This directory is archived and unsupported.

Lane manifests are no longer the active runtime source of truth. Local runtime settings now live in `.claw/settings.json` and `.claw/settings.local.json`.
